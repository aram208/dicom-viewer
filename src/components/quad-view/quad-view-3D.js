import { useEffect, useRef, useState } from 'react';
import { Container } from '@mui/material';

export default function VolumeViewer({ imageData, volumeRenderer }) {

    

    const setupVolumeRendering = () => {

    }



    return (
        <Container 
            sx={{ border: '1px solid yellow', width:'100%', height:'100%' }}>
            Hello from 3DView
        </Container>
    );
}

